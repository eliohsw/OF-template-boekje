\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{packages/set-tikz}[2025/02/15 v0.1.3 TikZ Settings]

\RequirePackage{tikz}
\RequirePackage{pgfplots}

\pgfplotsset{compat=1.18}

\usetikzlibrary{
    3d,
    angles,
    arrows.meta,
    calc,
    calligraphy,
    decorations.pathreplacing,
    patterns,
    perspective,
    positioning,
    quotes,
    shadows,
    shapes.geometric
}

\newcommand{\printStars}[1]{%
  \foreach \x in {1,...,#1}{\(\bigstar\)}%
}