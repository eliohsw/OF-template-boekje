# OF-template-boekje

A lightweight, multipurpose book template built for XeLaTeX.

This template supports multilingual documents via Polyglossia, but mainly handles Latin and CJK text.

To compile the document, simply type `make` or `make xelatexmk` in your terminal, or by clicking **Recompile** on Overleaf.
